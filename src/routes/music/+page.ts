import type { PageLoad } from './$types';
import { supabase } from "$lib/supabaseClient";


export const load = (async () => {
    console.log('hol');
    
    
    // return {
    //   pics: data ?? [],
    // };
    
}) satisfies PageLoad;